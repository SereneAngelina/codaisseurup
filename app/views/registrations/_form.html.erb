<%= form_for [@event, @event.registrations.new] do |f| %>
  <p><strong>Event Price:</strong> <span>€</span> <%= @event.price %></p>

  <hr>

  <div class="form-group">
    <label>Number Guests:</label>
    <%= f.select :guests_count, options_for_select(1..3) %>
  </div>


  <%= f.hidden_field :event_id, value: @event.id %>
  <%= f.hidden_field :price, value: @event.price %>

  <hr>

  <div class="actions">
    <%= f.submit "Book Now", class: "btn btn-success" %>
  </div>
<% end %>
